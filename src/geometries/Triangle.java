package geometries;

import primitives.Point;

/**
 *
 */
public class Triangle extends Polygon {
    /**
     *
     * @param a
     * @param b
     * @param c
     */
    public Triangle(Point a, Point b, Point c) {
        super(a, b, c);
    }
}
